<h3><%= @question.name %></h3>
<table class="table">
	<tr>
		<th>Benutzer</th>
		<th>Antwort</th>
	</tr>
	<% @question.answers.each do |answer| %>
		<tr>
			<td><%= answer.user.name %></td>
			<td><%= answer.text %></td>
		</tr>
	<% end %>
</table>
